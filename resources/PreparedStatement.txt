Connection conn = DBConnection.openConnection(); // Creates Connection
    String insertStatement = "INSERT INTO countries(Country, Create_Date, Created_By, Last_Updated_By) " +
            "VALUES (?,?,?,?)"; // ? Index begin at 1
    String updateStatement = "UPDATE country SET Country = ?, Create_Date = ? WHERE country = ?";
    String deleteStatement = "DELETE FROM countries WHERE Created_By = 'admin'";
    DBQuery.setPreparedStatement(conn, deleteStatement);
    PreparedStatement statement = DBQuery.getPreparedStatement();
    /*
    String country_name;
    String create_date = "2021-01-25 00:00:00";
    String created_by = "admin";
    String last_updated_by = "admin";

    // Get user input
    Scanner keyboard = new Scanner(System.in);
    System.out.print("Enter a country: ");
    country_name = keyboard.nextLine();

    // Key value mapping
    statement.setString(1, country_name);
    statement.setString(2, create_date);
    statement.setString(3, created_by);
    statement.setString(4, last_updated_by);
    */
    statement.execute();

    // Check rows affected
    if (statement.getUpdateCount() > 0){
        System.out.println(statement.getUpdateCount() + " rows affected!");
    }else{
        System.out.println("No Change.");
    }